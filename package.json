{
  "name": "crop-clip",
  "private": true,
  "version": "0.3.10",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check:layout": "node scripts/check-layout.mjs",
    "check:range": "npm run build --silent && node scripts/check-time-range.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "check:browser": "playwright test",
    "check:unit": "node scripts/check-state-normalization.mjs && node scripts/check-layout.mjs && node scripts/check-ffmpeg-lifecycle.mjs && node scripts/check-recording-cleanup.mjs && node scripts/check-recording-backpressure.mjs && node scripts/check-recording-navigation.mjs && node scripts/check-player-input.mjs && node scripts/check-release-notes.mjs && node scripts/check-time-range.mjs",
    "check": "npm run build && npm run check:unit && npm run check:browser"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/chrome": "^0.0.279",
    "esbuild": "^0.28.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@ffmpeg/core": "^0.12.10",
    "@ffmpeg/ffmpeg": "^0.12.15"
  }
}
